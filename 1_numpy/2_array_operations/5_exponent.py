import numpy as np

array = np.array([1, 2, 3, 4, 5])

# using the ** operator
result1 = array ** 2
print("Using the ** operator:", result1)

# using the np.power() function
result2 = np.power(array, 2)
print("Using the np.power() function:", result2)
